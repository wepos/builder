{
	SOURCE = Libft/src;
	COMPILED_FILES = pizda.c iii.c;
	IGNORE_FILE = ;
	INCLUDES_DIR = Libft/includes;
	COMPILER_OBJ = gcc -g;
	COMPILER_PR = ar rc libft.a;
	OUTPUT_DIR = "lib/";
}
{
	SOURCE = sources;
	COMPILED_FILES = ;
	IGNORE_FILE = ;
	INCLUDES_DIR = includes;
	COMPILER_OBJ = gcc -g -Llib -ILibft/includes;
	COMPILER_PR = gcc -g -o 42sh;
	OUTPUT_DIR = ;
}